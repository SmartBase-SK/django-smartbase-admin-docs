---
sidebar_position: 4
---
import IconCatalog from '@site/src/components/IconCatalog';

# Icons

Use the exact icon name as the value of the `icon` parameter.

```python
SBAdminMenuItem(view_id="dashboard", icon="All-application")
SBAdminCustomAction(title="Export to XLSX", icon="Excel-one", ...)
```

Some icons include spaces in their names, such as `Pin Filled`, and `Sort Alt`, so the value must match the icon name exactly.

## Browse Icons

The catalog below lets you filter icons by name and preview each one directly.

<IconCatalog />
